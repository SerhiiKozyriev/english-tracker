# Shared

Reusable, feature-agnostic building blocks live here.

- `components`: presentational components with no domain ownership.
- `directives`: reusable Angular directives.
- `pipes`: reusable Angular pipes.
- `types`: generic TypeScript contracts.
- `utils`: framework-light helpers.

Shared code must not depend on feature-specific code.
